<section
  class="py-16 px-[5%] bg-linear-to-b from-(--cream) to- relative flex items-center justify-center min-h-screen"
>
  <div class="max-w-[1400px] mx-auto w-full">
    <div class="text-center mb-16 fade-in">
      <h2
        class="font-display text-[clamp(2.5rem,5vw,4rem)] font-normal mb-4 text-(--dark)"
      >
        Packs de Servicio
      </h2>
      <p
        class="text-(--dark) text-[1.3rem] font-text opacity-70 font-medium mx-auto"
      >
        Te mostramos algunos packs que ofrecemos en Maguito Studio. Si tenés una
        idea en concreto y no se ajusta a ninguno de ellos, contactanos y nos
        contás cual es tu idea!
      </p>

      <button onclick="window.openModal()"
        class="mx-auto block inline-flex items-center justify-center whitespace-nowrap rounded-[9999px] border-4 shadow-[8px_8px_0_var(--dark)] transition-transform duration-300 transform hover:-translate-y-4 hover:-rotate-2 text-xl my-4"
        style="background:var(--cream); color:var(--dark); border-color:var(--dark); font-family:var(--font-display); padding: 4px 48px;"
        >¿Cual me conviene?
        </button>
    </div>

    <div class="flex flex-wrap gap-10 justify-center fade-in">
      <div
        class="bg-(--yellow) border-4 border-(--dark) rounded-[30px] py-8 px-6 sm:px-12 transition-all duration-400 ease-[cubic-bezier(0.68,-0.55,0.265,1.55)] relative shadow-[8px_8px_0_var(--dark)] service-card flex flex-col w-full sm:w-[calc(50%-20px)] lg:w-[calc(33.333%-27px)]"
      >
        <h3
          class="font-display text-[2rem] mb-4 text-(--dark) font-normal text-center"
        >
          Magia Total
        </h3>
        <p class="font-text font-semibold text-[1.5rem] mb-4">
          El pack más completo: Pensado para quienes buscan una experiencia
          integral.
        </p>
        <ul
          class="text-(--dark) leading-10 font-medium font-text text-[1.2rem] space-y-2 my-4 list-disc list-inside"
        >
          <li>Todo el pack <strong>Hechizo Creativo</strong></li>
          <li>Diseño y desarrollo de una landing page</li>
          <li>Kit básico para redes sociales</li>
          <li>Set extendido de mockups y aplicaciones de tu logo</li>
        </ul>
        <p
          class="text-[2rem] font-display text-(--dark) leading-7 font-medium my-8 text-center"
        >
          Precio: $400.000 ARS
        </p>

        <a href="#contacto"
          class="mx-auto block inline-flex items-center justify-center whitespace-nowrap rounded-[9999px] border-4 shadow-[8px_8px_0_var(--dark)] transition-transform duration-300 transform hover:-translate-y-4 hover:-rotate-2 text-xl"
          style="background:var(--yellow); color:var(--dark); border-color:var(--dark); font-family:var(--font-display); padding: 4px 48px;"
        >
          Contactar
        </a>
      </div>

      <div
        class="bg-(--orange) border-4 border-(--dark) rounded-[30px] py-8 px-6 sm:px-12 transition-all duration-400 ease-[cubic-bezier(0.68,-0.55,0.265,1.55)] relative shadow-[8px_8px_0_var(--dark)] service-card flex flex-col w-full sm:w-[calc(50%-20px)] lg:w-[calc(33.333%-27px)]"
      >
        <h3
          class="font-display text-[2rem] mb-4 text-(--dark) font-normal text-center"
        >
          Hechizo Creativo
        </h3>
        <p class="font-text font-semibold text-[1.5rem] mb-4">
          El pack más elegido: Ideal para marcas que quieren dar el paso de
          diferenciarse
        </p>
        <ul
          class="text-(--dark) leading-10 font-medium font-text text-[1.2rem] space-y-2 my-4 list-disc list-inside"
        >
          <li>Todo el pack <strong>Chispa Mágica</strong></li>
          <li>Manual de Marca Completo</li>
          <li>Kit básico para redes sociales (Destacadas, Plantillas, etc.)</li>
          <li>3 diseños de stickers o aplicaciones gráficas.</li>
        </ul>
        <p
          class="text-[2rem] font-display text-(--dark) leading-7 font-medium my-8 text-center"
        >
          Precio: $200.000 ARS
        </p>

        <a href="#contacto"
          class="mx-auto block inline-flex items-center justify-center whitespace-nowrap rounded-[9999px] border-4 shadow-[8px_8px_0_var(--dark)] transition-transform duration-300 transform hover:-translate-y-4 hover:-rotate-2 text-xl"
          style="background:var(--yellow); color:var(--dark); border-color:var(--dark); font-family:var(--font-display); padding: 4px 48px;"
        >
          Contactar
        </a>
      </div>

      <div
        class="bg-(--blue) border-4 border-(--dark) rounded-[30px] py-8 px-6 sm:px-12 transition-all duration-400 ease-[cubic-bezier(0.68,-0.55,0.265,1.55)] relative shadow-[8px_8px_0_var(--dark)] service-card flex flex-col w-full sm:w-[calc(50%-20px)] lg:w-[calc(33.333%-27px)]"
      >
        <h3
          class="font-display text-[2rem] mb-4 text-(--dark) font-normal text-center"
        >
          Chispa Mágica
        </h3>
        <p class="font-text font-semibold text-[1.5rem] mb-4">
          Ideal si estás comenzando y querés empezar de la mejor forma
        </p>
        <ul
          class="text-(--dark) leading-10 font-medium font-text text-[1.2rem] space-y-2 my-4 list-disc list-inside"
        >
          <li>Diseño de Branding completo</li>
          <li>Brandboard (Paleta de colores, logo, estilo visual)</li>
          <li>Diseño de tarjeta de presentación o 1 sticker</li>
          <li>Ejemplos para posts en redes</li>
        </ul>
        <p
          class="text-[2rem] font-display text-(--dark) leading-7 font-medium my-8 text-center"
        >
          Precio: $150.000 ARS
        </p>

        <a href="#contacto"
          class="mx-auto block inline-flex items-center justify-center whitespace-nowrap rounded-[9999px] border-4 shadow-[8px_8px_0_var(--dark)] transition-transform duration-300 transform hover:-translate-y-4 hover:-rotate-2 text-xl"
          style="background:var(--yellow); color:var(--dark); border-color:var(--dark); font-family:var(--font-display); padding: 4px 48px;"
        >
          Contactar
        </a>
      </div>
    </div>

    <img
      src="./Hand.svg"
      alt="Manito"
      class="w-md h-112 md:block hidden animate-bounce-custom rotate-12"
    />
  </div>
</section>

<style>
  /* minimal custom CSS for the fade-in entrance + bounce animation */
  .fade-in {
    opacity: 0;
    transform: translateY(50px);
    transition: all 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  .fade-in.visible {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes bounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  .animate-bounce-custom {
    animation: bounce 2s ease-in-out infinite;
  }

  /* responsive shadow tweak similar to previous CSS */
  @media (max-width: 768px) {
    .service-card {
      box-shadow: 6px 6px 0 var(--dark);
    }
    .service-card:hover {
      box-shadow: 8px 8px 0 var(--dark);
    }
  }
</style>
